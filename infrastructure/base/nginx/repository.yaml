# flux HelmRepository for installing nginx ingress controller
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: nginx
  namespace: nginx
spec:
  interval: 1h
  type: oci
  url: oci://registry.k8s.io/ingress-nginx